---
method: post
---

{% liquid
	graphql result = 'item/update', id: context.params.id ,title: context.params.title

	if result.record.id
		redirect_to '/'
	else
		echo 'Something went wrong'
		echo result.errors
	endif
%}