{% liquid
  graphql items = 'item/search_by_title'
  
  if item==null
  if result.record.id 
  graphql email = 'item/email/created', data: context.params
      redirect_to '/'
  else
      echo 'Something went wrong'
      echo result.errors
  endif
  endif
%}
%