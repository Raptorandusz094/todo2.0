mutation user_create(
  $name: String
) {
  record: record_create(
    record: {
      table: "user"
      properties: [
        { name: "name", value: $name }
      ]
    }
  ) {
    id
    name: property(name: "name")
  }
}