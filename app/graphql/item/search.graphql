query item_search {
  ingredient: records(
    per_page: 100
    filter: {
      table: { value: "item"  },
  
    }
    sort: {
      updated_at: { order: DESC }
    }
  ) {
    total_entries
    results {
      id
      
     
       }
     
    }
  }
